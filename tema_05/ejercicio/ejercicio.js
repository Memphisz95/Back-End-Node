// Tema 5
let altura_cm = 176;
let altuma_m = 1.76;
let peso_kg = 67.50;

let altura_redondeada = altuma_m.toFixed(1);
console.log(altura_redondeada);

let peso_redondeado = peso_kg.toPrecision(2);
console.log(peso_redondeado);

let max_valor = Number.MAX_VALUE == (Number.MAX_VALUE + 1);
console.log(max_valor);